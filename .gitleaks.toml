title = "Secure CI/CD Gitleaks Config"

[[rules]]
id = "aws-secret-access-key"
description = "Detect AWS secret access keys in environment-style files"
regex = '''AWS_SECRET_ACCESS_KEY\s*=\s*[A-Za-z0-9/+=]{20,}'''
tags = ["aws", "secret", "credential"]
