services:
  api:
    build: ./flask-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp